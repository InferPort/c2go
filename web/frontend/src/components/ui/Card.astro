---
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<div class={`bg-zinc-950/90 backdrop-blur-xl border border-zinc-800 rounded-2xl p-6 sm:p-10 shadow-2xl shadow-black/80 ${className}`}>
  <slot />
</div>
